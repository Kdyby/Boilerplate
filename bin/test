#!/bin/bash

DIR="$(dirname "$(readlink -f "$0")")/.."
TESTS_DIR="$DIR/tests"

# clean previouse errors
rm $TESTS_DIR/*.html &> /dev/null
rm $TESTS_DIR/*.log &> /dev/null

# goto testing dir
cd $TESTS_DIR

# run tests
phpunit --configuration "phpunit.xml"
